package com.shark.mybatisboot.common.model;

/**
 * 用于validated的分组
 */
public interface JsrGroup {

    interface Add { } ;

    interface Update { } ;

    interface Delete { } ;

    interface Get { } ;
}
